@* ProductList's default view *@

@model PaginatedList<ProductDto>

<div class="row gy-2">
    @foreach (ProductDto product in @Model.Items)
    {
        @await Component.InvokeAsync("ProductCard", product)
    }
</div>